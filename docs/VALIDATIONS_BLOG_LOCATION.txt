=== BLOGS COLLECTION ===

{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["title", "excerpt", "content", "authorId", "category"],
    "properties": {
      "title": {
        "bsonType": "string",
        "minLength": 10,
        "maxLength": 200,
        "description": "Заголовок статьи"
      },
      "excerpt": {
        "bsonType": "string",
        "minLength": 50,
        "maxLength": 500,
        "description": "Краткое описание статьи"
      },
      "content": {
        "bsonType": "string",
        "minLength": 200,
        "maxLength": 50000,
        "description": "Содержание статьи"
      },
      "authorId": {
        "bsonType": "objectId",
        "description": "ID автора статьи"
      },
      "category": {
        "enum": ["tips", "reviews", "news", "guides"],
        "description": "Категория статьи"
      },
      "image": {
        "bsonType": ["string", "null"],
        "maxLength": 2,
        "description": "Эмодзи/иконка статьи"
      },
      "readTime": {
        "bsonType": ["int", "null"],
        "minimum": 1,
        "maximum": 120,
        "description": "Время чтения в минутах"
      },
      "views": {
        "bsonType": ["int", "null"],
        "minimum": 0,
        "description": "Количество просмотров"
      },
      "status": {
        "enum": ["draft", "published"],
        "description": "Статус статьи"
      },
      "createdAt": {
        "bsonType": ["date", "null"],
        "description": "Дата создания"
      },
      "updatedAt": {
        "bsonType": ["date", "null"],
        "description": "Дата обновления"
      }
    },
    "additionalProperties": true
  }
}

=== LOCATIONS COLLECTION ===

{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["name", "type"],
    "properties": {
      "name": {
        "bsonType": "string",
        "minLength": 2,
        "maxLength": 100,
        "description": "Название локации"
      },
      "type": {
        "enum": ["city", "category"],
        "description": "Тип локации"
      },
      "icon": {
        "bsonType": ["string", "null"],
        "maxLength": 2,
        "description": "Эмодзи/иконка локации"
      },
      "adsCount": {
        "bsonType": ["int", "null"],
        "minimum": 0,
        "description": "Количество объявлений"
      },
      "createdAt": {
        "bsonType": ["date", "null"],
        "description": "Дата создания"
      },
      "updatedAt": {
        "bsonType": ["date", "null"],
        "description": "Дата обновления"
      }
    },
    "additionalProperties": true
  }
}

